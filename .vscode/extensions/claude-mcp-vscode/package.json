{
  "name": "claude-mcp-vscode",
  "displayName": "Commit Message AI",
  "description": "VSCode extension for AI-powered semantic commit message generation",
  "version": "0.1.0",
  "publisher": "local",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "claude-mcp-vscode.callMCP",
        "title": "Generate Commit Message",
        "icon": "$(robot)"
      },
      {
        "command": "claude-mcp-vscode.noOp",
        "title": "No Staged Changes",
        "icon": "$(circle-slash)"
      },
      {
        "command": "claude-mcp-vscode.showOutput",
        "title": "Commit Message AI: Show Output",
        "icon": "$(output)"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "claude-mcp-vscode.noOp",
          "group": "navigation@1",
          "when": "scmProvider == git && !claude-mcp-vscode.isGenerating && !claude-mcp-vscode.hasStagedChanges",
          "icon": "$(circle-slash)"
        },
        {
          "command": "claude-mcp-vscode.callMCP",
          "group": "navigation@2",
          "when": "scmProvider == git && !claude-mcp-vscode.isGenerating && claude-mcp-vscode.hasStagedChanges",
          "icon": "$(robot)"
        },
        {
          "command": "claude-mcp-vscode.noOp",
          "group": "navigation@3",
          "when": "scmProvider == git && claude-mcp-vscode.isGenerating",
          "icon": "$(sync~spin)"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.80.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {}
}
