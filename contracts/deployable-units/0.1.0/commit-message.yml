# Deployable Unit Contract: commit-message
# Version: 0.1.0

moniker: "commit-message"
name: "Commit Message Structure Contract"
type: "contract"
description: "Contract defining semantic commit message structure and format for mono-repository"
root: "contracts/commit-message/"
root_type: "contract-directory"

versioning:
  version_scheme: "MAJOR.MINOR.PATCH"
  changelog_path: "contracts/commit-message/CHANGELOG.md"

source:
  includes:
    - "**/*.yml"
    - "**/*.yaml"
    - "**/*.md"

depends_on: []
used_by:
  - "claude-config"
  - "src-mcp-vscode"
