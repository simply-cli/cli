moniker: "contracts-deployable-units"
name: "Deployable Unit Contracts"
type: "contracts"
description: "Contract definitions for deployable modules in the mono-repository"
root: "contracts/deployable-units/"
root_type: "contracts-directory"

versioning:
  version_scheme: "MAJOR.MINOR.PATCH"
  changelog_path: "contracts/deployable-units/CHANGELOG.md"

source:
  includes:
    - "**/*.yml"
    - "**/definitions.yml"

depends_on: []
