# Deployable Unit Contract: definitions
# Version: 0.1.0

moniker: "definitions"
name: "Repository location based configurations"
type: "definitions-type"
description: "foldable yaml file constructs that defines repository configurations"
root: "docs/guide/"
root_type: "single-docs-subsite"

versioning:
  version_scheme: "MAJOR.MINOR.PATCH"
  changelog_path: "/docs/guide/CHANGELOG.md"

source:
  includes:
    - "definitions.yml"
    - "**/definitions.yml"

depends_on: []
used_by:
  - "automation"
