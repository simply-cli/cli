# Deployable Unit Contract: src-mcp-github
# Version: 0.1.0

moniker: "src-mcp-github"
name: "GitHub MCP Server"
type: "mcp-server"
description: "Internal MCP server for GitHub API integration"
root: "src/mcp/github"
root_type: "single-go-service"
required_env: ['GITHUB_TOKEN']

versioning:
  version_scheme: "MAJOR.MINOR.PATCH"
  changelog_path: "/docs/CHANGELOG.md"

source:
  includes:
    - "src/mcp/github/go.mod"
    - "src/mcp/github/**.go"

depends_on: ['gh-cli']
