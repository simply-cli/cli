# Deployable Unit Contract: src-mcp-pwsh
# Version: 0.1.0

moniker: "src-mcp-pwsh"
name: "PowerShell MCP Server"
type: "mcp-server"
description: "Model Context Protocol server for executing PowerShell commands"
root: "src/mcp/pwsh"
root_type: "single-go-service"

versioning:
  version_scheme: "MAJOR.MINOR.PATCH"
  changelog_path: "/docs/CHANGELOG.md"

source:
  includes:
    - "src/mcp/pwsh/go.mod"
    - "src/mcp/pwsh/**.go"

depends_on: ['pwsh-shell']
